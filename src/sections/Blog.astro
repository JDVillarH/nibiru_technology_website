---
import Card from "../components/Card.astro";
import { postsList } from "../constants/data.js";
---

<section id="blog" class="section_container flex items-center flex-col">
  <div class="blog_text flex flex-col items-center text-center">
    <span class="outlined_subtitle red">Blog</span>
    <h2>Compartimos nuestro conocimiento</h2>
    <p>
      Si te apasiona la tecnología y el desarrollo web, has llegado al lugar
      correcto para estar al día y obtener consejos útiles para tus proyectos
      personales y profesionales.
    </p>
  </div>
  <div class="blog_container section_container">
    {postsList.map((post) => <Card post={post} />)}
  </div>
</section>

<style>
  #blog .blog_text {
    gap: 1rem;
    width: min(100%, 780px);
    margin-inline: auto;
  }

  #blog .blog_container {
    gap: 2rem;
    display: grid;
    justify-content: center;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 250px), 1fr));
    margin-top: 4rem;
  }

  @media (max-width: 1024px) {
    #blog .blog_text {
      align-items: flex-start;
      text-align: left;
    }
  }
</style>
