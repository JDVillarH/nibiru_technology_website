---
import Feature from "../components/Feature.astro";

import { featuresData } from "../constants/data.js";
---

<section id="features" class="section_container">
  <div class="features_text flex flex-col items-center text-center">
    <span class="outlined_subtitle red">Carácteristicas</span>
    <h2>Todo lo que necesitas para tu sitio web</h2>
    <p>
      A continuación, te presentamos algunas carácteristicas diseñadas para
      optimizar la construcción y la efectividad de tu proyecto en un mundo cada
      vez más digital.
    </p>
  </div>
  <div class="features_content">
    {featuresData.map((feature) => <Feature features={feature} />)}
  </div>
</section>

<style>
  #features .features_text {
    gap: 0.5rem;
    width: min(100%, 780px);
    margin-inline: auto;
  }

  #features .features_content {
    gap: 3rem;
    display: grid;
    margin-top: 5rem;

    @media screen and (min-width: 992px) {
      .features_section:nth-child(odd) article {
        order: 1;
      }

      .features_section:nth-child(even) article {
        margin-left: auto;
        ul {
          text-align: right;
        }
      }
    }
  }

  @media (max-width: 1024px) {
    #features .features_text {
      align-items: flex-start;
      margin-inline: initial;
      text-align: left;
    }
  }
</style>
